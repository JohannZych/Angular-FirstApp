export class Cocktail {
  name: string;
  price: number;
  img: string;

  constructor(name: string, price: number, image: string) {
    this.name = name;
    this.price = price;
    this.img = image;
  }
}
